```c
#include<stdio.h>
int main()
{
    long n,m=0,max,min;
    scanf("%ld",&n);    //??????
    max=min=n%10;  //??????
    if (n==0)
    {
        m=1,max=0,min=0;  //??0
    }
    else
    {
        while (n)    //??0
        {
            if(max<n%10) max=n%10;       
            else if(min>n%10) min=n%10;
            m++,n/=10;        //?????????????
        }
        
    }
    printf("%ld %ld %ld\n",m,max,min);
    return 0;    
}
```
