```c
#include<stdio.h>
int main()
{
    int i,sum=0;
    float n,aver,x;
    int a[101];
    scanf("%f\n",&n);
    for ( i = 0; i < n; i++)
    {
        scanf("%d",&a[i]);
    }
    for ( i = 0; i < n; i++)
    {
        sum+=a[i];
    }
   
    aver=sum/n;
    printf("%.2f",aver);
    return 0;
}   
```
