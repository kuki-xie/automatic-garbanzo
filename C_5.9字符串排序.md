```c
#include<stdio.h>
#include<string.h>
void sort(char a[],int n){
	int i,j;
	for(j=0;j<n-1;j++){
		for(i=0;i<n-j-1;i++){
			if(a[i]>a[i+1]){
			char t=a[i];
			a[i]=a[i+1];
			a[i+1]=t;
		 	}
		}
	}
}
int main(){
	char a[80];
	int n;
	while(1){
		scanf("%s",a);
		n=strlen(a);
		if(n==0)	break;
		sort(a,n);
		printf("%s\n",a);
	}
	return 0;
}
//错误


#include<stdio.h>
#include<string.h>
void sort(char a[],int n){
	int i,j;
	for(j=0;j<n-1;j++){
		for(i=0;i<n-j-1;i++){
			if(a[i]>a[i+1]){
			char t=a[i];
			a[i]=a[i+1];
			a[i+1]=t;
		 	}
		}
	}
}
int main(){
	char a[80],ch;
	int n,i;
	while(1){
		scanf("%c",&ch);
		if(ch=='\n')    break;
		a[0]=ch;
        i=1;
        while(1){
            scanf("%c",&a[i]);
            if(a[i]=='\n')  break;
            i++;
        }
        a[i]='\0';
        sort(a,i);
        printf("%s\n",a);	
    }	
	return 0;
}
/* 问题描述
编写一个程序，对用户输入的字符串（不含空格，不超过80个字符），将其字符按ASCII码值升序排列后再输出。
输入说明
输入多行数据，每行为一个字符串，当输入空行（字符串长度为0）时结束。
输出说明
输出每个处理后的字符串，然后换行。
输入样例
样例1输入
abdcfe
Aac19f

输出样例
样例1输出
abcdef
19Aacf  */

```
