```c
#include<stdio.h>
int main()
{
	int sort(int b[],int n);
	int a[31];
	int i,t,n,count;
	scanf("%d", &t);
	for (int i = 0; i < t; i++)
		{
			scanf("%d", &a[i]);
		}
		sort(a, t);
		for (int i = 0; i < t; i++)
		{
			count = 1;
			printf("%d:", a[i]);
			while (i < t&&a[i] == a[i + 1])
			{
				count++;
				i++;
			}
			printf("%d\n", count);
		}
	
	return 0;
		
}
int sort(int b[],int n)
{
	int temp;
	for (int i = 0; i < n-1; i++)
		for (int j = 0; j < n - 1 - i;j++)
			if (b[j]>b[j + 1])
			{
				temp = b[j];
				b[j] = b[j + 1];
				b[j + 1] = temp;
			}
	return 0;
}
```
