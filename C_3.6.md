```c
#include<stdio.h>
int main()
{
    int x,t,a,n=0,sum=0;
    scanf("%d",&x);
    t=x;

    for ( int i = 0; i < 9; i++)
    {
        if (t==0) break;
        t/=10;
        n++;            //????
    }
    for (int i = 1; i <=n; i++)
    {
        a=x%10;
        sum+=a;        
        x/=10;        //????????
    }
    printf("%d\n",sum);
    return 0;
    
}
```
