```c
//自己写的是错的
#include<stdio.h>
int main(void)
{
  void sort(int num[],int N);
  int num[20];
  int N,i;
  scanf("%d",&N);
  for(i=0;i<N;i++)
    scanf("%d",&num[i]);
  sort(num,N);
  for(i=0;i<N;i++)
    printf("%d ",num[i]);
  return 0;
}

void sort(int*num,int N)
{
  int i,j,n,temp;
  int x=0,y=0;
  int a[20],b[20];
  for(i=0;i<N;i++)
  {
    if(num[i]%2 == 0)
    {
        a[x]=num[i];
        x++;
    }
    else
    {
        b[y]=num[i];
        y++;                 //分奇偶数组
    }
  }
  for(i=0;i<y-1;i++)
  {
    for(j=0;j<y-1-i;j++)
    {
        if(b[j] > b[j+1])       //奇数
        {temp = b[j];b[j]=b[j+1];b[j+1]=b[j];}    //从小到大排序
    }     
  }
  for(i=0;i<x;i++) num[i]=b[i]; 
  for(i=0;i<x-1;i++)
  {
    for(j=0;j<x-1-i;j++)
    {                            //偶数
        if(a[j] > a[j+1])
        {temp = a[j];a[j]=a[j+1];a[j+1]=a[j];}
    }     
  }
  for(i=0;i<x;i++) num[i+y]=a[i];  
}
/* 问题描述
定义一个一维整形数组num[20]，输入正整数N（N≤20）代表num的实际元素个数，输入N个任意整数到num中，编写函数sort()实现排序处理，完成前面奇数，后面偶数，分别从小到大排序。使用指针完成地址传递，主函数完成N、数组元素的输入和处理后的数组元素输出。
输入说明
输入正整数N（N≤20）和N个任意排列的整形到一维数组num中。
输出说明
格式输出：数据之间空一格，奇数和偶数分界处空三个空格。
输入样例
5
2 3 7 8 4
输出样例
3 7 2 4 8 */


#include<stdio.h>
int sort(int a[],int n){
    int i,j,k;
    int b[20],c[20];
    for(i=0;i<n-1;i++){
        for(j=0;j<n-1;j++){
            if(a[j]>a[j+1]){
                int temp=a[j];
                a[j]=a[j+1];
                a[j+1]=temp;
            }
        }
    }
    j=0;k=0;
    for(i=0;i<n;i++){
        if(a[i]%2==1)   b[j++]=a[i]; //j是奇数个数
        else            c[k++]=a[i]; //k是偶数个数
    }
    for(i=0;i<j;i++)    a[i]=b[i];
    for(i=j;i<n;i++)    a[i]=c[i-j];
    return j; //返回奇数个数
}
int main(){
    int i,j;
    int N,num[20];
    scanf("%d",&N);
    for(i=0;i<N;i++)    scanf("%d",&num[i]);
    j=sort(num,N);
    for(i=0;i<j;i++)    printf("%d ",num[i]);
    printf("   ");
    for(i=j;i<N;i++)    printf("%d ",num[i]);
    return 0;
}

#include<stdio.h>
void paiXu(int n,int num[]){
	int brige;
	for(int i=0;i<n;i++){
		for(int j=0;j<n-1-i;j++){
			if(num[j]>num[j+1]){
			    brige=num[j];
			    num[j]=num[j+1];
			    num[j+1]=brige;
		    }
		}
	}
} 
void sort(int n,int num[]){
	int jiShu[20];
	int ouShu[20];
	int number_of_jiShu=0;
	int number_of_ouShu=0;
	
	//ÕÍ≥…∆Ê ˝∫Õ≈º ˝µƒ∑÷¿‡ 
	for(int i=0;i<n;i++){
		if(num[i]%2!=0){
			jiShu[number_of_jiShu]=num[i];
			number_of_jiShu++;
		}
		else{
			ouShu[number_of_ouShu]=num[i];
			number_of_ouShu++;
		}
	}
	paiXu(number_of_jiShu,jiShu);
	paiXu(number_of_ouShu,ouShu);
	
	for(int i=0;i<number_of_jiShu;i++){
		num[i]=jiShu[i];
	}
	int j;
	for(int i=number_of_jiShu,j=0;j<number_of_ouShu;j++){
		num[i]=ouShu[j];
		i++;
	}
}
int main()
{
	int n;
	int num[20];
	scanf("%d",&n);
	for(int i=0;i<n;i++){
		scanf("%d",&num[i]);
	}
	sort(n,num);
	for(int i=0;i<n;i++){
		printf("%d ",num[i]);
	}
	
	
 } 


#include<stdio.h>
int sort(int a[],int n){
    int i,j,k;
    int b[20],c[20];
    for(i=0;i<n-1;i++){
        for(j=0;j<n-1-i;j++){
            if(a[j]>a[j+1]){
                int temp=a[j];
                a[j]=a[j+1];
                a[j+1]=temp;
            }
        }
    }
    j=0;k=0;
    for(i=0;i<n;i++){
        if(a[i]%2==1)   b[j++]=a[i]; //j是偶数个数
        else            c[k++]=a[i]; //k是奇数个数
    }
    for(i=0;i<j;i++)    a[i]=b[i];
    for(i=j;i<n;i++)    a[i]=c[i-j];
    return j; //返回奇数个数
}
int main(){
    int i,j;
    int N,num[20];
    scanf("%d",&N);
    for(i=0;i<N;i++){
        scanf("%d",&num[i]);
    }
    j=sort(num,N);
    for(i=0;i<j;i++){
        printf("%d ",num[i]);
    }
    printf("   ");
    for(i=j;i<N;i++){
        printf("%d ",num[i]);
    }
    return 0;
}
